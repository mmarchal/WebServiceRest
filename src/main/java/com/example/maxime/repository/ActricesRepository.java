package com.example.maxime.repository;

import com.example.maxime.entities.Actrices;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ActricesRepository extends JpaRepository<Actrices, Long> {
}
