package com.example.maxime.repository;

import com.example.maxime.entities.DessinsAnimes;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DessinsAnimesRepository extends JpaRepository<DessinsAnimes, Long> {
}
