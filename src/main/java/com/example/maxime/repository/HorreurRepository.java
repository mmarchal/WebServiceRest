package com.example.maxime.repository;

import com.example.maxime.entities.Horreur;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HorreurRepository extends JpaRepository<Horreur, Long> {
}
