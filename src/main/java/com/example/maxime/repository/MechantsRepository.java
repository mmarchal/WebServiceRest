package com.example.maxime.repository;

import com.example.maxime.entities.Mechants;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MechantsRepository extends JpaRepository<Mechants, Long> {
}
