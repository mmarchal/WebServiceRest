package com.example.maxime.repository;

import com.example.maxime.entities.Rappeurs;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RappeursRepository extends JpaRepository<Rappeurs, Long> {
}
